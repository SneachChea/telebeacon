# uvpkg

Minimal Python package illustrating usage of `uvloop`.

## Installation

```bash
cd /Users/shaheenacheche/workspace/telebeacon
python -m pip install .
```

This will install the package and the `uvpkg` console script.

## Usage

```bash
uvpkg
# prints "Hello from uvloop!"
```
